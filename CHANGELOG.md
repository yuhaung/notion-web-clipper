# 更新日志

本文件记录 **Notion & Feishu & Obsidian Web Clipper** 的版本变更。
脚本本体见 [`notion-web-clipper.user.js`](./notion-web-clipper.user.js)，
安装与配置见 [README.md](./README.md)。

> 版本说明同时以行内注释形式保留在脚本头部（自 `v5.16.x` 起），两处互为参照。

---

## v5.25.0

外部审查报告复核后的修复专项（5 项修复 + 3 项驳回），另补两项分发与文档改进。

- **修复**：失败面板按 `Esc` 关闭时绕过二次确认。v5.20.0 已为关闭按钮加了确认
  （关闭等于丢弃整篇剪藏的内容快照，不可恢复），但 `Esc` 路径落在兜底分支、
  直接隐藏面板——既无确认也不清空快照。同一个面板、两条关闭路径行为不一致，
  误按 `Esc` 即静默丢失内容。已抽出 `closeErrPanel()` 供两条路径共用。
- **修复**：Obsidian 探测遇瞬态错误不重试。探测返回非 200/404 时抛出的 `Error`
  未挂 `status`，而 `withRetry` 依赖 `status` 判定可重试性，于是 500/502/503
  一次都不重试、直接判死——插件重启一下就能成功的写入被报成永久失败。
  补挂 `status`，同时把该处手写的 `retryOn`（只认 `network` / `423` / `500`）
  改用通用 `isRetryableError`（`network` / `429` / 全部 5xx）再并上 `423`；
  两处必须同改，否则 502/503/429 依旧漏网。
- **修复**：Markdown 输出中嵌套列表与引用的续行缩进丢失。换行替换原先是硬编码的
  两个/三个空格（或 `> `），未累加 `indent`；顶层看不出问题，嵌套时续行会少掉
  外层缩进量，渲染回来被当成新的顶层段落。
- **修复**：超长文本分片丢失链接与格式。内容超过 `TEXT_SAFE` 时只有第一片携带
  `link` 与 `annotations`，后续分片退化为纯文本——长链接文本的后半段在 Notion 中
  不可点，加粗等格式也一并丢失。改为逐片继承。
- **修复**：`textFromBlocks` 对表格的冗余递归。表格分支已取完全部行内容，
  末尾仍拿同一批 `table_row` 再递归一次，而 `table_row` 既无 `rich_text`、
  也不匹配任何分支、更无子节点，整趟递归不产出任何文本，纯属空转。
- **改进**：`Ctrl+A` 全选改为优先取 `ShadowRoot` 自身的 `getSelection()`（若实现）
  再回退主文档。预览区位于 closed Shadow Root 内，`window.getSelection()` 跨边界
  设置选区会被重定向到 host，Firefox 下直接无效，表现为「按 `Ctrl+A` 什么都没选中」。
- **新增**：脚本头部补上 `@downloadURL` 与 `@updateURL`，指向本仓库 Raw 的
  `notion-web-clipper.user.js`。此前这两项缺失，Tampermonkey 无从得知脚本托管位置，
  用户只能手动重装。
- **新增**：本文件（`CHANGELOG.md`）。此前 `@description` 声称
  「变更日志见 … `CHANGELOG.md`」，但该文件并不存在，属死引用；
  现将版本历史从 README 抽离为独立文件，README 保留版本徽章与指向本文件的链接。

> ⚠️ 已在用的 v5.24.0 及更早版本**没有** `@updateURL` 元数据，需手动重装一次本版本，
> 之后才能自动接收后续更新。

**已驳回的外部建议（3 项）**：报告所称三处「断行损伤」（`parseResponseHeader` 的正则、
`isUserBlocked` 与 `matchDomainTags` 的分隔符）经比对源码均正确无误，系其粘贴/传输损伤
而非源码缺陷；报告建议调换 `richToMd` 的 `code` 与 `bold` 包裹顺序，但当前「先 code 后 bold」
外层最后应用的是 bold、输出 `` **`text`** ``（加粗的代码，符合 CommonMark），
照其建议调换反而会把星号关进代码块内、变成字面量。上述四项均已加入回归断言与变异测试，
防止后人照报告改坏。

---

## v5.24.0

飞书发送链路缺陷修复专项（6 处 + 1 项驳回）：

- 修复 `rootDegradeAppended` 从未声明——每一次飞书发送都会在内容全部写完后抛
  `ReferenceError`，文档已落库却报「发送失败」，且崩溃会被误判为「嵌套插入失败」，
  进而把整棵子树重写一遍。
- 修复图片上传阶段的「停止」被吞、流程走完却谎报「发送完成」。
  **上述两项必须同批修**——第一项的崩溃恰好掩盖了第二项，只修第一项会让失败模式
  从「报错」退化为「静默谎报成功」。
- 修复中止后重试重跑已落库的批次，导致文本块重复。
- 修复降级子树拿子树下标去删根文档里无关的正文块。
- 修复降级块数跨轮丢失，使续传边界前移、静默丢内容。
- 中止后回收本次新建的半篇文档（移入回收站，可恢复；续传而来的文档不碰）。

驳回一项外部建议：飞书 `batch_delete` 的 `start_index` / `end_index` 属请求体而非 query，
源码即官方口径。

本轮另补上验证体系的结构性盲区——既有 309 条断言全是源码文本匹配、不做作用域检查，
故新增静态哨兵扫描未声明标识符。

---

## v5.23.0

独立交叉审查后的缺陷修复专项（16 处，5 名审查员分块复核 1591~3811 行）：

- 修复「链接里的图片 / 视频被整块丢弃」（最高危——`<a href><img></a>` 里的媒体永远走不到
  IMG 分支，WordPress 图库、知乎、公众号、灯箱组件普遍中招，等于配图全没）。
- 修复 Notion 分块体积估算系统性偏小（`table_row` 的内容在 `cells` 里被漏算，且按
  1 字符 1 字节估中文实为 3 字节，大表格请求体超限被拒、重试原样复现，该页永久发不出去）。
- 修复进度分子重复累加导致虚高甚至提前钳到 100%。
- 修复包装 Error 时丢弃 `status` / `code` / `network`（401 不再提示改凭据、
  Obsidian 连不上插件被判成不可重试）。
- 修复配置导出白名单漏 `ENABLE_FEISHU`（导出再导入会静默关掉飞书）。
- 修复删光所有块仍可发送、凭空建出空页面。
- `gmRequest` 补协议白名单（`file://` 的 hostname 是空串，曾既不命中 localhost
  也不命中私网而被放行）。
- 修复 `mapLimit` 并发上限未校验（算出 `NaN` 时所有条目被静默跳过，
  表现为「图片全丢却报成功」）。

另修：嵌套父块未创建时丢整棵子树、段内换行丢失、iframe 内松手拖拽态卡死、
懒加载占位图选不中、飞书建文档不透传 signal 导致云盘留空文档等。

---

## v5.22.0

缺陷修复专项（8 处）：

- 修复「停止发送后重试会重复建页 / 建文档」——**这是 v5.20.0 自己引入的回归**，
  四处取消分支都在对账逻辑之前提前抛出，导致 `resume` 恒为 null，
  点重试就重新 POST 建新页，上一轮已写入的半篇留在库里没人接管；
  改为取消与失败走同一条对账路径。
- 修复「没有可发送的平台」分支漏复位，会留下标题 / 标签永久只读、按钮永久 disabled
  的卡死态且无任何自愈路径。
- 修复取消按钮永久 disabled（重试打开面板后只剩 Esc 能退出）。
- 修复总进度条永不显示（CSS 默认 `display:none`，而代码赋的是空串、
  只删掉内联声明回落到 none）。
- 修复 `Alt+Shift+N` 在 iframe 编辑器内误触发。
- 停止提示不再无条件声称「可点重试从断点继续」——根本没有断点时那是句空话，
  改按是否真有 resume 区分。

---

## v5.21.0

知乎选取逻辑专项：

- 修复 host 判定误判——`hostname.includes('zhihu.com')` 会把 `evilzhihu.com`、
  `zhihu.com.evil.com` 一并判为知乎，从而在非知乎页面上执行知乎专属清洗，
  剪藏结果被无声破坏且无从察觉（`includes('x.com')` 更宽，
  `ox.com`、`x.company.com` 全部命中）；改为「精确匹配或子域」。
- 修复 ↓ 键是死键——候选序列恒为「越往后越大」，`↓` 永远被 clamp 在 0，
  点中整张回答卡后无法再缩小到正文；新增向内候选（`.RichText` 等），
  默认仍是「点一下选整卡」，缩小是可选动作而非默认行为。
- 修复评论区解析失败时静默降级成通用克隆、把投票按钮和时间戳当正文产出垃圾块。
- 修复选中正文时源链接拿不到（问题标题在目标之上）。
- 新增折叠长回答检测并提示「建议先点『阅读全文』再提取」——只提示不自动点击，
  展开会触发懒加载并改变滚动位置，属于用户决策。

---

## v5.20.0

发送流程重做（业务与存储契约不变）：

- 修复进度条长时间冻结（用户反馈的「卡在 70% 不动」）——根因是块写入与图片上传
  两个上报器挂在同一批次循环内交替触发，而单调守卫把后发的块写入上报全吞掉；
  改为统一工作计量，进度成为「已完成单元 / 总单元」这一个单调函数。
- 修复发送中无法取消：新增 `NcAbort` 与可中断 `sleep`，`AbortController` 贯穿全链路，
  只拦「下一步」而不 abort 在飞请求；对账请求刻意不接 signal，
  以便取消后仍能读回真实写入量。取消不计入 errors 而单列 aborted，
  两者都可从断点续传。
- 修复只启用 Notion 时进度永远差最后一格。
- 失败信息可操作化：`mkSendError` 保留结构化字段，错误面板按平台分组、
  长文本折叠而非硬截断，凭据类错误给出直达设置的入口。
- 进度改为分平台多行——总进度是各平台的加权平均，
  暴露不出「A 已完成而 B 卡在 20%」。

---

## v5.19.0

正确性修复与热路径优化：

- 修复 Obsidian 写入静默覆盖——探测目标笔记是否存在时把「请求失败」与「文件不存在」
  都当作不存在，网络抖动或插件瞬时 5xx 时会用原路径直接 PUT，
  覆盖 vault 中已有同名笔记且**不可撤销**；改为三态判定
  （404 → 原路径 / 200 → 时间戳路径 / 其余 → 中止写入）。
- 修复 `listItemBlock` 内 walk 深度被重置为 0，导致深度上限对列表项内的深层嵌套
  完全失效（每层列表项都能再递归 60 层，深嵌套 DOM 可撑爆调用栈）。
- 私网 IP 判定补齐 `240.0.0.0/4` 保留段、广播地址与三个文档段，
  并修正 fail-closed 的形态校验——`999.1.1.1` 等越界四段曾命中形态分支后跳过
  fail-closed、被判为公网，从而绕过 SSRF 复核。
- 性能：解析阶段按 annot 对象身份缓存序列化键、节点计数改用 TreeWalker
  越过上限即停（整页选取从 O(n) 降为 O(limit)）、飞书按批预分组消除
  O(批数 × 任务数) 空转、预览删块改增量重排（不再全量重建导致图片重新解码与
  滚动位置丢失）。

---

> ⚠️ **v5.8.0 ~ v5.18.0 的变更说明已不可考** —— 这些版本均通过 GitHub 网页端直接上传，
> 提交信息为默认的「Update file」，原始变更记录未留存。
> 若你保留了当时的本地副本，欢迎补充。

---

## v5.7.1

修复 Ctrl+Enter 连按导致整份内容双发；修复 IPv6 内网地址绕过 SSRF 防护；
Twitter 会话提取增规模护栏 + `Alt+单击` 单条模式；平台未配置时不再残留选取模式；
失败弹窗「复制 Markdown」扩展至任意平台；坏图 30s 负缓存；
Obsidian 写入重试幂等化（防换名双写）；补充 CSDN / 掘金 / 少数派 / 网易图床白名单；
多项防御性加固。

## v5.7.0

新增「发送节奏」档位（温和 / 标准）；修复弹窗被阻止误报；
Obsidian 未配置 API Key 不再默认发送；深层 DETAILS / 列表递归深度护栏；
Markdown front matter 补转义。

## v5.6.0

选取模式 ↑/↓ 调整范围；新增「禁用站点」黑名单与「域名默认标签」。

## v5.5.0

修复监听累积泄漏、重试后快捷键失效、PRE/TABLE 悬停被外层抢占；
复制 / Markdown 补齐表格与折叠块；推文正文剔除操作栏计数；
图片缓存增总字节上限；新增追加模式、发送中防误关页面、进度条单调推进、
重试保留已成功平台。
